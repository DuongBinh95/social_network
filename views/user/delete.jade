extends layout

block content
    if message
        h2#{message}
    else
        h1 Do you want to delete this user?
        h2 "#{posts.name}"
        form(method="post").form-inline
            button(type="submit").btn.btn-danger Delete
            a(href="/").btn.btn-default Cancel